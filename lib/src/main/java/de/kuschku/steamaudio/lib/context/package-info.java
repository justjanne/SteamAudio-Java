/**
 * <h1>Context</h1>
 * <p>
 * Defines a Context object, which controls low-level operations of Phonon. Typically, a Context is specified once
 * during the execution of the client program, before calling any other API functions. Once any API function is called,
 * changing the Context may lead to undefined behavior.
 */
package de.kuschku.steamaudio.lib.context;